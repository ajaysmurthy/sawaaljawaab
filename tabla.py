## Table analysis and synthesis module for the HAMR 2015 ISMIR hack

import os
import essentia as es
import numpy as np
import pickle

strokes = {'dha', }

def buildStrokeModels():
    

def testModule(inputFile, pulsePos):
    outFile = 'outTabla.wav'
    
    opulsePos = ipulsePos
    return outFile, opulsePos

if __name__ == "__main__":
    outFile = testModule(inputFile = 'inTabla.wav',ipulsePos = None)
    print "Stored output file to %s" %outFile
    
